import defaultTheme from "./themedefault";

/*Either define a new theme, or modify default theme */

// const theme = cloneDeep(defaultTheme);
// theme.palette.primary = ["#f00"];
// theme.palette.secondary = ["#0f0"];

export default defaultTheme;
